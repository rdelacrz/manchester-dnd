# t_3799221d implementation notes

- Consolidated `ProtectedLayout` to render the shared `AuthenticatedLayout` rather than duplicating the authenticated shell.
- Moved the responsive player navigation, active-route state, account label, and logout control into `SideNavigation` so authenticated routes share one shell implementation.
- Reused `PublicLayout` for the guide/privacy/legal information pages, removing their duplicate header/footer/main-shell assembly while preserving the global skip-link target (`#main-content`).
- Extended `PublicHeader` with signed-out account actions while keeping the public navigation separate from player navigation.
- Updated `auth-navigation.spec.ts` to assert the extracted public/auth layout contract across desktop and narrow responsive projects without depending on the character-library empty-state test id.

Verification evidence:

- `cargo test --locked -p manchester-dnd-app --features ssr -- --nocapture` — PASS, 27 lib + 6 bin tests.
- `cargo test --locked -p manchester-dnd-app` — PASS, freshly re-run after the Task 2 split commit; 7 lib tests + doc tests.
- `cargo leptos build --release` — PASS, release SSR binary and hydrate bundle built.
- `npx playwright test tests/browser/auth-navigation.spec.ts --config=tests/browser/config/playwright.config.ts` — PASS, 48/48 across desktop Chromium/Firefox/WebKit and mobile Chromium/Firefox/WebKit.
- `graphify update .` — PASS, graph refreshed.

Commit status:

- Created commit `0a4b88b refactor: extract public and authenticated layouts` after controller approval to split-stage only the pure Task 2 files.
- The commit includes `app/src/components/{layout,protected_layout,public_header,side_navigation}.rs`, `app/src/components/info.rs`, and `tests/browser/auth-navigation.spec.ts`.
- Left mixed later-task files (`app/src/app.rs`, `app/src/components/mod.rs`, `style/main.css`) unstaged/uncommitted as instructed.
