[package]
name = "manchester-dnd-core"
version = "0.1.0"
edition = "2024"

[lib]
name = "manchester_dnd_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"

